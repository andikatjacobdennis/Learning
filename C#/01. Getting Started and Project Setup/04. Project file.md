# Common `.csproj` Configurations

The `.csproj` file controls how a .NET project is built, packaged, and published.

---

## Basic Project Template

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
</Project>
```

---

## Essential Properties

| Property | Options | Description |
|----------|---------|-------------|
| `TargetFramework` | `net8.0`, `net9.0`, `net10.0` | Target .NET version |
| `OutputType` | `Exe`, `Library`, `WinExe` | Application type |
| `Nullable` | `enable`, `disable` | Enable nullable reference types |
| `ImplicitUsings` | `enable`, `disable` | Enable global usings |

```xml
<PropertyGroup>
  <TargetFramework>net10.0</TargetFramework>
  <OutputType>Exe</OutputType>
  <Nullable>enable</Nullable>
  <ImplicitUsings>enable</ImplicitUsings>
</PropertyGroup>
```

---

## Important Properties

| Property | Options | Description |
|----------|---------|-------------|
| `LangVersion` | `preview`, `latest`, `12.0` | C# language version |
| `TreatWarningsAsErrors` | `true`, `false` | Treat warnings as errors |
| `GenerateDocumentationFile` | `true`, `false` | Generate XML documentation |
| `NoWarn` | `CS1591;CS8600` | Suppress specific warnings |
| `AssemblyName` | Any string | Output assembly name |
| `RootNamespace` | Any string | Default namespace |

```xml
<PropertyGroup>
  <LangVersion>preview</LangVersion>
  <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  <GenerateDocumentationFile>true</GenerateDocumentationFile>
  <NoWarn>CS1591</NoWarn>
  <AssemblyName>MyApp</AssemblyName>
  <RootNamespace>MyCompany.MyApp</RootNamespace>
</PropertyGroup>
```

---

## Multiple Target Frameworks

```xml
<PropertyGroup>
  <TargetFrameworks>net8.0;net10.0</TargetFrameworks>
</PropertyGroup>
```

---

## References

### Package References (NuGet)

```xml
<ItemGroup>
  <PackageReference Include="Serilog.AspNetCore" Version="9.0.0" />
  <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.0-preview.1" />
</ItemGroup>
```

### Project References

```xml
<ItemGroup>
  <ProjectReference Include="..\MyApp.Domain\MyApp.Domain.csproj" />
  <ProjectReference Include="..\MyApp.Application\MyApp.Application.csproj" />
</ItemGroup>
```

### Framework References

```xml
<ItemGroup>
  <FrameworkReference Include="Microsoft.AspNetCore.App" />
</ItemGroup>
```

---

## File Handling

### Copy Files to Output

```xml
<ItemGroup>
  <None Update="appsettings.json">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </None>
</ItemGroup>
```

### Exclude Files

```xml
<ItemGroup>
  <Compile Remove="OldCode\**\*.cs" />
</ItemGroup>
```

### Include Additional Files

```xml
<ItemGroup>
  <Content Include="Templates\**\*" />
</ItemGroup>
```

---

## Publishing

| Property | Options | Description |
|----------|---------|-------------|
| `PublishSingleFile` | `true`, `false` | Single executable file |
| `SelfContained` | `true`, `false` | Include .NET runtime |
| `PublishTrimmed` | `true`, `false` | Trim unused code |
| `RuntimeIdentifier` | `win-x64`, `linux-x64`, `osx-arm64` | Target platform |

```xml
<PropertyGroup>
  <PublishSingleFile>true</PublishSingleFile>
  <SelfContained>true</SelfContained>
  <PublishTrimmed>true</PublishTrimmed>
  <RuntimeIdentifier>win-x64</RuntimeIdentifier>
</PropertyGroup>
```

---

## Conditional Configuration

```xml
<PropertyGroup Condition="'$(Configuration)' == 'Debug'">
  <DefineConstants>DEBUG</DefineConstants>
  <Optimize>false</Optimize>
</PropertyGroup>

<PropertyGroup Condition="'$(Configuration)' == 'Release'">
  <DefineConstants>RELEASE</DefineConstants>
  <Optimize>true</Optimize>
</PropertyGroup>
```

---

## Common SDKs

| SDK | Use Case |
|-----|----------|
| `Microsoft.NET.Sdk` | Console apps, class libraries, tests |
| `Microsoft.NET.Sdk.Web` | Web APIs, MVC, Blazor |
| `Microsoft.NET.Sdk.Worker` | Background services |
| `Microsoft.NET.Sdk.Razor` | Razor class libraries |

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">
```

---

## User Secrets (Development Only)

```xml
<PropertyGroup>
  <UserSecretsId>c8d6d3c7-7d2d-4b2d-a2b4-xxxxxxxxxxxx</UserSecretsId>
</PropertyGroup>
```

---

## Complete Examples

### Console Application

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>true</SelfContained>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Serilog" Version="9.0.0" />
    <ProjectReference Include="..\MyApp.Core\MyApp.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="appsettings.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
```

### Web API

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.0-preview.1" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.0-preview.1" />
    <ProjectReference Include="..\MyApp.Application\MyApp.Application.csproj" />
  </ItemGroup>

</Project>
```

### Class Library

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="9.0.0" />
  </ItemGroup>

</Project>
```

---

## Quick Reference Table

| Element | Purpose | Example |
|---------|---------|---------|
| `TargetFramework` | .NET version | `net10.0` |
| `OutputType` | App type | `Exe`, `Library`, `WinExe` |
| `Nullable` | Null safety | `enable` |
| `ImplicitUsings` | Global usings | `enable` |
| `PackageReference` | NuGet packages | `Serilog.AspNetCore` |
| `ProjectReference` | Project dependencies | `..\MyApp.Domain\` |
| `FrameworkReference` | Framework dependencies | `Microsoft.AspNetCore.App` |
| `None Update` | Copy files | `appsettings.json` |
| `PublishSingleFile` | Single executable | `true` |
| `SelfContained` | Include runtime | `true` |
| `RuntimeIdentifier` | Target OS | `win-x64` |